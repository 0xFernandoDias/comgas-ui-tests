import React from 'react'
import { useForm } from '../../contexts/FormContext'
import { Box } from '@chakra-ui/react'

export const Checkout: React.FC = () => {
  const { state } = useForm()

  // modal
  // SEND DATA DB

  return <Box>Hello Checkout</Box>
}
